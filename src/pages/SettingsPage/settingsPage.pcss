.settings {
  width: 30rem;
  margin: 2rem auto 0 auto;
  display: flex;
  flex-direction: column;
  align-items: center;

  &__avatar {
    border-radius: 9999px;
    height: 12rem;
    aspect-ratio: 1;
    object-fit: cover;
    margin-bottom: 1rem;
  }
}
